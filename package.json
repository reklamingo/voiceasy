{
  "name": "voiceasy-capacitor-wrapper",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo \"No web assets needed; using remote URL\"",
    "cap:ios": "npx cap add ios || true && npx cap sync ios"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2"
  }
}